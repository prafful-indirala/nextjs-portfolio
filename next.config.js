/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable TypeScript
  typescript: {
    // Enable type checking during production build (optional)
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
